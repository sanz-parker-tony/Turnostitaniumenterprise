/**
 * Attendance > Calc Results
 * Resultados de cálculos de asistencia
 */

'use client';

import ScreenPageShell from '@/components/ScreenPageShell';

export default function AttendanceCalcResultsPage() {
  return (
    <ScreenPageShell
      screenKey="ATT_CALC_RESULTS"
      title="Resultados de Cálculo"
      description="Resultados de cálculos de asistencia"
    />
  );
}
