/**
 * Attendance > Process Runs
 * Ejecuciones de procesos de asistencia
 */

'use client';

import ScreenPageShell from '@/components/ScreenPageShell';

export default function AttendanceProcessRunsPage() {
  return (
    <ScreenPageShell
      screenKey="ATT_PROCESS_RUNS"
      title="Ejecuciones de Procesos"
      description="Historial de ejecuciones de procesos de asistencia"
    />
  );
}
