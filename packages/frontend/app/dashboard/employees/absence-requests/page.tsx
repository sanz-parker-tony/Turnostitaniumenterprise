/**
 * Employees > Absence Requests
 * Solicitudes de ausencia
 */

'use client';

import ScreenPageShell from '@/components/ScreenPageShell';

export default function EmployeesAbsenceRequestsPage() {
  return (
    <ScreenPageShell
      screenKey="EMPL_ABSENCE_REQUESTS"
      title="Solicitudes de Ausencia"
      description="Gestión de solicitudes de ausencia y permisos"
    />
  );
}
