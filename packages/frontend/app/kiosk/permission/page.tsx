/**
 * /kiosk/permission - Solicitar Permisos
 * Ruta protegida (requiere autenticación previa)
 */

'use client';

import KioskPermission from '@/components/kiosk/KioskPermission';

export default function KioskPermissionPage() {
  return <KioskPermission />;
}
