/**
 * Organization > Companies
 * Gestión de empresas
 */

'use client';

import ScreenPageShell from '@/components/ScreenPageShell';

export default function OrgCompaniesPage() {
  return (
    <ScreenPageShell
      screenKey="ORG_COMPANIES"
      title="Empresas"
      description="Gestión de empresas del tenant"
    />
  );
}
