/**
 * Config > Attendance Processes
 * Gestión de procesos de asistencia
 */

'use client';

import ScreenPageShell from '@/components/ScreenPageShell';

export default function ConfigAttendanceProcessesPage() {
  return (
    <ScreenPageShell
      screenKey="CONF_ATT_PROCESS"
      title="Procesos de Asistencia"
      description="Gestión de procesos de cálculo de asistencia"
    />
  );
}
